import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fleetwatch.installer',
  appName: 'Installer UI',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
