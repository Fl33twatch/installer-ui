// src/pages/RegisterUnit.tsx
import React from "react";
import { Redirect } from "react-router-dom";

const RegisterUnit: React.FC = () => <Redirect to="/install/new" />;

export default RegisterUnit;
